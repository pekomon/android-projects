name: LockBox CI

on:
  push:
    paths:
      - 'LockBox/**'
      - '.github/workflows/lockbox-ci.yml'
  pull_request:
    paths:
      - 'LockBox/**'
      - '.github/workflows/lockbox-ci.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: LockBox
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup JDK
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v4

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build and run tests
        run: ./gradlew :app:testDebugUnitTest :app:assembleDebug :app:compileDebugAndroidTestKotlin

      - name: Upload debug APK
        uses: actions/upload-artifact@v7
        with:
          name: lockbox-debug-apk
          path: LockBox/app/build/outputs/apk/debug/app-debug.apk
